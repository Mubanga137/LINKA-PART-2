'use client';

import { useAuth } from '@/contexts/auth-context';
import { useRetailerAuth } from '@/contexts/retailer-auth-context';

export function AuthStatusBanner() {
  // Remove all auth status messaging from homepage for clean, professional look
  // Homepage should be universally accessible without status indicators
  return null;
}
